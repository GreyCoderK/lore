version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused

linters-settings:
  errcheck:
    exclude-functions:
      - "fmt.Fprint"
      - "fmt.Fprintf"
      - "fmt.Fprintln"
      - "(io.Writer).Write"
      - "os.WriteFile"
    check-blank: false
